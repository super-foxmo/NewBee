server:
  port: 8030

# 应用名称
spring:
  application:
    name: newbee-mall-cloud-recommend-web
  # 连接nacos
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  #      username: nacos
  #      password: nacos
  datasource:
    url: jdbc:mysql://localhost:3306/newbee_mall_cloud_recommend_db?useUnicode=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8&autoReconnect=true&useSSL=false&allowMultiQueries=true
    username: root
    password: li970511